#include <stdio.h>
#include <stdlib.h>
#include "point.h"
/*
int main()
{
    POINT P;
    P = MakePoint(1, 3);
    printf("%f,%f\n", GetAbsis(P), GetOrdinat(P));
    float jarak;
    jarak = Jarak0(P);
    printf("%f", jarak);
    return 0;
}
*/
